// Framer Motion
import { navbarMotion, socialsMotion, sectionWrapperMotion, heroMotion, cardMotion, } from "./motion";
import { getSystemTheme, getTheme, isDarkTheme, systemThemeChangeHandler, lowerCase, setProjectsCount, rem } from './utils';

export {
  // Framer Motion
  navbarMotion, socialsMotion, sectionWrapperMotion, heroMotion, cardMotion,
  // Utils
  getSystemTheme, getTheme, isDarkTheme, systemThemeChangeHandler, lowerCase, setProjectsCount, rem
}
